---
import Layout from '../layouts/Layout.astro';
import StoreHeader from '../components/StoreHeader.astro';
import StoreGrid from '../components/StoreGrid.astro';
---

<Layout>
	<main>
		<div class="animate-fade-in" style="animation-delay: 0.1s;">
			<StoreHeader />
		</div>
		
		<div class="hero animate-fade-in" style="animation-delay: 0.3s;">
			<div class="hero-content">
				<h1 class="main-title">Tu Tienda <span class="accent-text">Valorant</span></h1>
				<p class="subtitle">Consulta tus skins diarias sin entrar al juego.</p>
			</div>
		</div>

		<div class="animate-fade-in" style="animation-delay: 0.5s;">
			<StoreGrid />
		</div>

		<div class="footer-note animate-fade-in" style="animation-delay: 0.8s;">
			<p>Este sitio no está afiliado con Riot Games.</p>
		</div>
	</main>
</Layout>

<style>
	main {
		min-height: 100vh;
		padding-bottom: 4rem;
	}

	.hero {
		padding: 4rem 2rem;
		text-align: center;
		position: relative;
		overflow: hidden;
	}

	.hero::before {
		content: '';
		position: absolute;
		top: 50%;
		left: 50%;
		transform: translate(-50%, -50%);
		width: 150%;
		height: 150%;
		background: radial-gradient(circle, rgba(255, 70, 85, 0.05) 0%, rgba(15, 25, 35, 0) 70%);
		z-index: -1;
	}

	.main-title {
		font-size: 3.5rem;
		margin: 0;
		line-height: 1;
	}

	.subtitle {
		font-size: 1.2rem;
		opacity: 0.7;
		margin-top: 1rem;
		letter-spacing: 1px;
	}

	.footer-note {
		text-align: center;
		margin-top: 4rem;
		opacity: 0.3;
		font-size: 0.8rem;
		text-transform: uppercase;
	}

	@media (max-width: 768px) {
		.main-title {
			font-size: 2.5rem;
		}
	}
</style>
